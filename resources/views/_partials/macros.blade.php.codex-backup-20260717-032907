@php
  // Semua pemanggil lama tetap kompatibel: height/width dipakai sebagai ukuran
  // kotak agar logo OSM tidak gepeng seperti ketika dipaksa ke rasio logo M.
  $size = $height ?? $width ?? '32';
@endphp

<span class="d-inline-flex align-items-center justify-content-center flex-shrink-0">
  <img src="{{ asset('assets/img/favicon/favicon-osm.png') }}"
    width="{{ $size }}" height="{{ $size }}" alt="OSM" class="rounded-2" loading="eager">
</span>
