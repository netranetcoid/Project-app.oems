@extends('layouts.contentNavbarLayout')
@section('title','Master Divisi')
@section('content')<div class="card"><div class="card-header d-flex justify-content-between align-items-center"><div><h4 class="mb-1">Master Divisi</h4><p class="text-muted mb-0">Setiap divisi ditautkan jelas ke PT OSM dan bila perlu ke Branch/Site penanggung jawab.</p></div>@can('division.create')<a class="btn btn-primary" href="{{ route('master.divisions.create') }}">Tambah Divisi</a>@endcan</div><div class="card-body">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->has('division'))<div class="alert alert-danger">{{ $errors->first('division') }}</div>@endif<div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Kode</th><th>Divisi</th><th>Company / Branch-Site</th><th>Penanggung Jawab</th><th>Status</th><th></th></tr></thead><tbody>@forelse($divisions as $division)<tr><td><code>{{ $division->code }}</code></td><td><strong>{{ $division->name }}</strong><div class="small text-muted">{{ $division->parent?->name ? 'Induk: '.$division->parent->name : ($division->type ?: '-') }}</div></td><td><strong>{{ $division->company?->name }}</strong><div class="small text-muted">{{ $division->branch?->parent?->name ? $division->branch->parent->name.' → ' : '' }}{{ $division->branch?->name ?: 'Lintas seluruh PT OSM' }}</div></td><td>{{ $division->head_name ?: '-' }}</td><td><span class="badge bg-label-{{ $division->status==='active'?'success':'secondary' }}">{{ ucfirst($division->status) }}</span></td><td class="text-nowrap">@can('division.update')<a class="btn btn-sm btn-label-warning" href="{{ route('master.divisions.edit',$division) }}">Edit</a>@endcan @can('division.delete')<form class="d-inline" method="POST" action="{{ route('master.divisions.destroy',$division) }}" onsubmit="return confirm('Hapus divisi ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-label-danger">Hapus</button></form>@endcan</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">Belum ada divisi.</td></tr>@endforelse</tbody></table></div>{{ $divisions->links() }}</div></div>@endsection
