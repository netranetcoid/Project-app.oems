@extends('layouts.contentNavbarLayout')
@section('title', 'Master Branch / Site')
@section('content')
<div class="card"><div class="card-header d-flex justify-content-between align-items-center"><div><h4 class="mb-1">Master Branch / Site</h4><p class="text-muted mb-0">Struktur operasional: PT OSM → Branch → Site. Tiap unit menjadi dasar tanggung jawab payroll, keuangan, pegawai, dan absensi.</p></div>@can('branch.create')<a class="btn btn-primary" href="{{ route('master.branches.create') }}"><i class="ti ti-plus me-1"></i>Tambah Branch / Site</a>@endcan</div>
<div class="card-body">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->has('branch'))<div class="alert alert-danger">{{ $errors->first('branch') }}</div>@endif<div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Kode</th><th>Branch / Site</th><th>Induk / Company</th><th>PIC</th><th>Status</th><th></th></tr></thead><tbody>@forelse($branches as $branch)<tr><td><code>{{ $branch->code }}</code></td><td><strong>{{ $branch->name }}</strong><div class="small text-muted">{{ ucfirst(str_replace('_',' ',$branch->type)) }}</div></td><td>@if($branch->parent)<strong>{{ $branch->parent->name }}</strong><div class="small text-muted">Site di bawah Branch</div>@else<strong>{{ $branch->company?->name }}</strong><div class="small text-muted">Langsung di bawah PT OSM</div>@endif</td><td>{{ $branch->pic_name ?: '-' }}<div class="small text-muted">{{ $branch->pic_phone }}</div></td><td><span class="badge bg-label-{{ $branch->status==='active'?'success':($branch->status==='closed'?'danger':'warning') }}">{{ ucfirst($branch->status) }}</span></td><td class="text-nowrap">@can('branch.update')<a href="{{ route('master.branches.edit',$branch) }}" class="btn btn-sm btn-label-warning">Edit</a>@endcan @can('branch.delete')<form class="d-inline" method="POST" action="{{ route('master.branches.destroy',$branch) }}" onsubmit="return confirm('Hapus Branch / Site ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-label-danger">Hapus</button></form>@endcan</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">Belum ada Branch / Site.</td></tr>@endforelse</tbody></table></div>{{ $branches->links() }}</div></div>
@endsection
